For markers:

dev.txt and test.txt have been placed in the root of this repo

BestModel contains:
- the final model's checkpoint
- a copy of the notebook that produced this model

src contains:
- utils for loading and cleaning the data
- EDA notebook
- the main notebook used when training the model